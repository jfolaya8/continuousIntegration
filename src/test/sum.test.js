test('adds 1 + 2 to equal 3', () => {
    expect(1 + 2).toBe(3);
});

test('adds 4 + 4 to equal 8', () => {
    expect(4 + 4).toBe(8);
});

test('adds 4 * 10 to equal 40', () => {
    expect(4 * 10).toBe(40);
});